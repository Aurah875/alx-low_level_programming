#include "main.h"
#include <stdio.h>

/**
 * mul - mulplies int a and int b
 * @a: - the first integer
 * @b: - the second integer
 * Return: always 0 (sucess)
 */

int mul(int a, int b)
{
int result = a * b;
return (result);
}
